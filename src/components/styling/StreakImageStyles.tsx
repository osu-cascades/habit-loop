import styled from 'styled-components/native';

export const View = styled.View`
    justify-content: center;
    align-items: center;
    resize-mode: contain;
`;

export const FlameImgBg = styled.ImageBackground`
    width: 220;
    height: 200;
    position: relative;
    text-align: center;
    top: 0;
    left: -30;
`;

export const Text = styled.Text`
    font-size: 40px;
    font-weight: bold;
    color: #FFFFFF;
    align-self: center;
    position; absolute;
    top: 85;
    left: 25;
`;