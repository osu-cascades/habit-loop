import React, { Component } from 'react';
import { Container, Header, Content, Tab, Tabs } from 'native-base';
import { Text } from 'react-native';

export default class MostConsistent extends Component {
  render() {
    return (
      <Container>
        <Text>MostConsistent</Text>
      </Container>
    );
  }
}